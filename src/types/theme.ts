export type ThemeMode = 'dark';

export interface ThemeTokens {
  bgPrimary: string;
  bgSecondary: string;
  bgTertiary: string;
  bgElevated: string;
  borderSubtle: string;
  borderStrong: string;
  accentPrimary: string;
  accentHover: string;
  textPrimary: string;
  textSecondary: string;
  textMuted: string;
}
