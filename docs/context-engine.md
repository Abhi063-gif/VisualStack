# Context Engine Pipeline

The `ContextEngine` harvests state automatically without manual prompt engineering:
- **Designer Scene Graph**: Selected elements, active viewport bounds.
- **Workflow State**: Active React Flow logic nodes and node connections.
- **Git State**: Current branch, uncommitted files count, remote origin URL.
- **DevOps & Container State**: History count, configured domains, active Docker containers.
